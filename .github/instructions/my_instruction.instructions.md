---
applyTo: '**'
---
1. 每次完成任务后，务必清理临时测试文件，保持项目结构整洁。文档应统一放在 `docs` 目录下，不要保留过多零散文档，必要时进行合并。
2. 每次更新代码后，检查 `README.md` 是否与实际功能一致。如不一致，确认是否需要修改；必要时进行更新。
3. 如有必要，可以执行 git 提交，保持版本管理完整。
4. 所有指令必须用中文回复。
5. 在处理任务时，保持工作目录干净，避免生成无用文件或冗余内容。
